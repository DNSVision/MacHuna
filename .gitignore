build/
dist/
__pycache__/
*.spec
!MacHuna.spec
*.pyc
.DS_Store

# Personal Claude Code overrides (permissions etc.) - not shared
.claude/settings.local.json

# Website publish staging - built by publish.sh, never committed
publish/

# Cloudflare publish token lives in ~, never here - belt and braces
.machuna_publish_token
